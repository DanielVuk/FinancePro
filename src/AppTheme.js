import { createTheme } from "@mui/material/styles";

export let AppTheme = createTheme({
    palette: {
        primary: {
            main: "#5D2DFD",
        },
        background: {
            default: "#F1ECFD",
        },
    },
});
